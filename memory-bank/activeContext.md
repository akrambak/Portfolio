# Active Context

*   **Current work focus:** Full redesign around an "instrument panel" direction — a precision-datasheet aesthetic (hairline rules, monospace data, tabular numerals, one rationed accent) that stays sober for technical recruiters while converting client leads. Positioning moved to AI-first: Claude-SDK agents wired into production Laravel/PrestaShop systems, with eight years of e-commerce backend work as the proof rather than the pitch.
*   **Recent changes:** Migrated Tailwind to v4 CSS-first tokens (the old `tailwind.config.ts` was never loaded, so every `accent-*` class and all `prose` styling was silently dead). Nav cut from seven routes to four; `/portfolio`, `/modules` and `/themes` fold into `/work` with 308 redirects and server-side category filters. Fabricated module/theme catalogues removed. Dropped `framer-motion`, `react-icons`, `CursorGlow` and the purple→pink body gradient in favour of CSS-only motion and inline SVG. Added SEO (sitemap, robots, JSON-LD, generated OG image), MDX syntax highlighting, reading time, prev/next, a 404 page, and a honeypot on the contact form.
*   **Next steps:** Add real client case studies to `src/content/work.ts`, fill LinkedIn/GitHub/Calendly in `src/config/site.ts`, wire an email provider into `/api/contact`, and confirm the production domain in `site.url`.
*   **Active decisions and considerations:** Colour lives entirely in tokens — never add a `dark:` variant, add or reuse a token. Anything unset in `src/config/site.ts` renders nothing, so dead CTAs cannot ship. Filters are `searchParams`-driven to stay shareable and JS-free.
